<%= form_with(model: setup, local: true) do |form| %>
  <% if setup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(setup.errors.count, "error") %> prohibited this setup from being saved:</h2>

      <ul>
      <% setup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :website_id %>
    <%= form.text_field :website_id, id: :setup_website_id %>
  </div>

  <div class="field">
    <%= form.label :average_bill %>
    <%= form.number_field :average_bill, id: :setup_average_bill %>
  </div>

  <div class="field">
    <%= form.label :yandex_net_fee %>
    <%= form.number_field :yandex_net_fee, id: :setup_yandex_net_fee %>
  </div>

  <div class="field">
    <%= form.label :yandex_share %>
    <%= form.number_field :yandex_share, id: :setup_yandex_share %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
