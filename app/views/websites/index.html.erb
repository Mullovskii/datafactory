<div class="container">
<p id="notice"><%= notice %></p>

<h1>Пайплайн продаж</h1>

<table class="table table-nonfluid">

  <thead>
    <tr>
      <th>Страна</th>
      <th>Сайт</th>
<!--       <th>Company</th>
 -->      <th>Категория</th>
   <!--    <th>Description</th>
      <th>Contacts</th>
      <th>Monthly visits</th>
      <th>Country rank</th> -->
<!--       <th>Valid</th>
 -->    <!--   <th>Status</th>
      <th>Bounce rate</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @websites.each do |website| %>
      <tr>
        <td><%= website.country.name %></td>
        <td><%= website.url %></td>
<!--         <td><%= website.company %></td>
 -->        <td><%= website.category %></td>
       <!--  <td><%= website.description %></td>
        <td><%= website.contacts %></td>
        <td><%= website.monthly_visits %></td>
        <td><%= website.country_rank %></td>
        <td><%= website.valid_for_yandex %></td>
        <td><%= website.status %></td>
        <td><%= website.bounce_rate %></td> -->
        <td><%= link_to 'Показать', website %></td>
        <td><%= link_to 'Править', edit_website_path(website) %></td>
        <td><%= link_to 'Удалить', website, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Добавить сайт', new_website_path %>
</div>
