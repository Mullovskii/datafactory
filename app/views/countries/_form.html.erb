<%= form_with(model: country, local: true) do |form| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :country_name %>
  </div>

  <div class="field">
    <%= form.label :sales_region %>
    <%= form.check_box :sales_region, id: :country_sales_region %>
  </div>

  <div class="field">
    <%= form.label :importer_region %>
    <%= form.check_box :importer_region, id: :country_importer_region %>
  </div>

  <div class="field">
    <%= form.label :market_volume %>
    <%= form.number_field :market_volume, id: :country_market_volume %>
  </div>

  <div class="field">
    <%= form.label :users %>
    <%= form.number_field :users, id: :country_users %>
  </div>

  <div class="field">
    <%= form.label :shoppers %>
    <%= form.number_field :shoppers, id: :country_shoppers %>
  </div>

  <div class="field">
    <%= form.label :game_market %>
    <%= form.number_field :game_market, id: :country_game_market %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :country_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
